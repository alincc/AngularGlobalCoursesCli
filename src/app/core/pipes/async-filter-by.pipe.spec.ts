import { AsyncFilterByPipe } from './async-filter-by.pipe';

describe('AsyncFilterByPipe', () => {
  it('create an instance', () => {
    const pipe = new AsyncFilterByPipe();
    expect(pipe).toBeTruthy();
  });
});
