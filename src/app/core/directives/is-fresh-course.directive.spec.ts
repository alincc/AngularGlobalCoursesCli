import { IsFreshCourseDirective } from './is-fresh-course.directive';

describe('IsFreshCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new IsFreshCourseDirective();
    expect(directive).toBeTruthy();
  });
});
